[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short --disable-socket --strict-markers
markers =
    live_mongo: opt-in read-only checks against a real MongoDB server
filterwarnings =
    ignore::DeprecationWarning
